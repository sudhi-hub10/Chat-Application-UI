<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chat Application UI</title>
  <style>
    body {
      margin: 0;
      font-family: "Poppins", sans-serif;
      background-color: #f2f2f2;
      display: flex;
      height: 100vh;
    }

    /* Sidebar */
    .sidebar {
      width: 25%;
      background-color: #0A192F;
      color: white;
      display: flex;
      flex-direction: column;
    }

    .sidebar h2 {
      text-align: center;
      padding: 15px 0;
      margin: 0;
      background-color: #112D4E;
    }

    .chat-list {
      flex: 1;
      overflow-y: auto;
      padding: 10px;
    }

    .chat-user {
      padding: 10px;
      border-bottom: 1px solid #1E3A5F;
      cursor: pointer;
    }

    .chat-user:hover {
      background-color: #1B2A45;
    }

    /* Main Chat Area */
    .chat-area {
      flex: 1;
      background-color: #fff;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .chat-header {
      background-color: #007BFF;
      color: white;
      padding: 15px;
      font-weight: 600;
    }

    .chat-messages {
      flex: 1;
      padding: 20px;
      overflow-y: auto;
    }

    .message {
      margin: 10px 0;
      padding: 10px 15px;
      border-radius: 12px;
      max-width: 60%;
      line-height: 1.4;
    }

    .sent {
      background-color: #DCF8C6;
      align-self: flex-end;
      margin-left: auto;
    }

    .received {
      background-color: #E9E9EB;
      align-self: flex-start;
    }

    .chat-input {
      display: flex;
      border-top: 1px solid #ccc;
      padding: 10px;
      background-color: #f8f8f8;
    }

    .chat-input input {
      flex: 1;
      padding: 10px;
      border-radius: 20px;
      border: 1px solid #ccc;
      outline: none;
      font-family: inherit;
    }

    .chat-input button {
      background-color: #007BFF;
      border: none;
      color: white;
      padding: 10px 18px;
      margin-left: 10px;
      border-radius: 20px;
      cursor: pointer;
    }

    .chat-input button:hover {
      background-color: #0056b3;
    }

    /* Scrollbar */
    ::-webkit-scrollbar {
      width: 6px;
    }
    ::-webkit-scrollbar-thumb {
      background: #aaa;
      border-radius: 4px;
    }

    @media (max-width: 768px) {
      .sidebar {
        display: none;
      }
    }
  </style>
</head>
<body>

  <div class="sidebar">
    <h2>💬 Chats</h2>
    <div class="chat-list">
      <div class="chat-user">🧑 John</div>
      <div class="chat-user">👩 Emma</div>
      <div class="chat-user">🧔 Michael</div>
      <div class="chat-user">👧 Lisa</div>
    </div>
  </div>

  <div class="chat-area">
    <div class="chat-header">Chat with John</div>

    <div class="chat-messages">
      <div class="message received">Hey! How are you?</div>
      <div class="message sent">I'm good, bro! Working on our new project UI 😎</div>
      <div class="message received">Looks awesome! Is it responsive?</div>
      <div class="message sent">Yup 🔥 It adjusts to mobile perfectly.</div>
    </div>

    <div class="chat-input">
      <input type="text" placeholder="Type a message...">
      <button>Send</button>
    </div>
  </div>

</body>
</html>
